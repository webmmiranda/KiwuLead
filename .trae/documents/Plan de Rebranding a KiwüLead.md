Entendido. Para evitar complicaciones y riesgos, limitaremos el cambio a un **Rebranding Visual**. Esto es seguro, rápido y no romperá nada.

### Plan Simplificado (Sin Riesgos)

1.  **Interfaz de Usuario (UI)**:
    *   Buscar y reemplazar "NexusCRM" y "Nexus CRM" por "**KiwüLead**" en todos los archivos de la interfaz (`src/**/*.tsx`, `index.html`).
    *   Esto actualizará títulos, encabezados, pies de página y mensajes de bienvenida.

2.  **Metadatos y Configuración**:
    *   Actualizar el nombre en `package.json` y `manifest.json` para que la aplicación se instale y muestre como "KiwüLead".
    *   Actualizar textos visibles en el instalador (`public/install/index.php`) y plantillas de correo.

3.  **Lo que NO tocaremos (Para seguridad)**:
    *   **NO** cambiaremos nombres de base de datos.
    *   **NO** cambiaremos claves de sesión (nadie perderá su sesión).
    *   **NO** cambiaremos los IDs de los formularios (los formularios en sitios de clientes seguirán funcionando perfectamente).

Este enfoque te da la nueva marca "KiwüLead" visiblemente en todo el sistema, manteniendo el motor "Nexus" por debajo para garantizar estabilidad total.
